{
  "name": "cap-bookshop",
  "version": "1.0.0",
  "description": "SAP CAP service for the Bookshop domain",
  "private": true,
  "dependencies": {
    "@sap/cds": "^8",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^1"
  },
  "scripts": {
    "start": "cds-serve",
    "watch": "cds watch",
    "build": "cds build"
  },
  "cds": {
    "requires": {
      "db": { "kind": "sqlite", "credentials": { "url": "db.sqlite" } }
    }
  }
}